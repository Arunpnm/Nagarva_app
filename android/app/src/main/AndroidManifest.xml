<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.nagarva.app"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:label="Nagarva"
        tools:replace="android:label"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Displays an Android View that continues showing the launch screen
                 Drawable until Flutter paints its first frame, then this splash
                 screen fades out. A splash screen is useful to avoid any visual
                 gap between the end of Android's launch screen and the painting of
                 Flutter's first frame. -->
            <meta-data
              android:name="io.flutter.embedding.android.SplashScreenDrawable"
              android:resource="@drawable/launch_background"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep linking -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="true" />
            <!-- Stale, pre-rename leftover (still literally "arunpkrs") and never
                 wired to any Dart code — left as-is per instruction, flagged here
                 for a rename to something Nagarva-branded next time this file is
                 touched. Not the scheme email confirmation now uses; see below. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="arunpkrs" android:host="arunpkrs.com" />
            </intent-filter>
            <!-- Email confirmation deep link (16 Aug 2026). The
                 link.nagarva.in confirmation landing page forwards to
                 nagarva://auth-callback#access_token=...&refresh_token=...&type=signup
                 — caught here and handled in main.dart via the app_links
                 package (getInitialLink for cold start, uriLinkStream for warm
                 resume). No autoVerify: that flag is for Android App Links
                 (Digital Asset Links over https), meaningless for a custom
                 scheme like this one. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nagarva" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />




    </application>

    <!-- Android 11+ (API 30, this app's targetSdkVersion is 36) filters
         package-visibility queries by default — without this, AndroidIntent's
         canResolveActivity()/launch() (signup_success_dialog.dart's "Open
         Email App" button) would see no matching activity even when a real
         email app is installed, since the OS hides other apps' components
         from a query unless explicitly declared here. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_EMAIL" />
        </intent>
    </queries>
</manifest>
